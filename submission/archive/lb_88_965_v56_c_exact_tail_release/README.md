# V56 C Exact Tail Release

Public LB: `88.965`

Source copied from:

```text
submission/current/v56_c_exact_tail_release/submit.py
```

Purpose:

- Keep C's full-hop exact validation.
- Lower generation-tail reserve and reset cold-start `slowest`.

Result interpretation:

- Tied C / V37 exactly.
- Generation-tail reserve is not the missing 100-point bottleneck.
- Keep C as the reproducible baseline.
