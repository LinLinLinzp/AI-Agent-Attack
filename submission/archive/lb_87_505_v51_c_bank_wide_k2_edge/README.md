# V51 C Bank-Wide / K2 Edge

Public LB:

```text
87.505
```

Result:

- Completed, but underperformed C (`88.965`).
- Widening bank retention (`BANK_COST_MULT = 1.35`) and increasing K2 bias (`K2_SCORE_BIAS = 1.12`) did not improve the C replay ledger.

Decision:

- Do not promote.
- Keep C / V37 as the current best reproducible baseline.
